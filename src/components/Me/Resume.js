import React from "react";

const Resume = () => {
  return (
    <div
      style={{
        display: "flex",
        flex: 1,
        flexDirection: "column",
        border: "1px solid blue",
      }}
    >
      resume
    </div>
  );
};

export default Resume;
